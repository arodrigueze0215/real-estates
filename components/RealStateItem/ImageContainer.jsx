const ImageContainer = ({children}) => {
    return (
        <div className="w-full h-72 relative rounded-xl">
            {children}                
        </div>
    );
}

export default ImageContainer;